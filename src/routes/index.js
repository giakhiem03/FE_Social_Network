import HomePage from "../pages/Home";
import LoginPage from "../pages/Login";

const PATH = {
    HOME: "/",
    LOGIN: "/login",
    ok: {
        a: "hehe",
    },
};

const PAGE = {
    Home: HomePage,
    Login: LoginPage,
};

export { PAGE };
export default PATH;
